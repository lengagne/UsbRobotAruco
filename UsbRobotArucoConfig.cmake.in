# - Config file for the USBROBOTARUCO lib
# It defines the following variables
#
# USBROBOTARUCO_INCLUDE_DIRS - include directories
# USBROBOTARUCO_LIBRARY_DIRS - library directory (normally not used!)
# USBROBOTARUCO_LIBRARIES - libraries to link against

# Tell the user project where to find our headers and libraries

SET(USBROBOTARUCO_INCLUDE_DIRS @USBROBOTARUCO_INCLUDE_DIRS@)

SET(USBROBOTARUCO_LIBRARY_DIRS @INSTALL_LIB_DIRS)

SET(USBROBOTARUCO_LIBRARIES @USBROBOTARUCO_LIBRARIES@)

SET(USBROBOTARUCO_FLAGS @USBROBOTARUCO_FLAGS@ )
